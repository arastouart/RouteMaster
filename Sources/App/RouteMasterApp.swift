import SwiftUI

// Phase 1 stub. The full glassmorphism UI + MenuBarExtra is implemented in Phase 6.
@main
struct RouteMasterApp: App {
    var body: some Scene {
        WindowGroup {
            Text("RouteMaster — scaffold")
                .frame(minWidth: 480, minHeight: 320)
        }
    }
}
